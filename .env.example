# Optional. src/config.js already holds the project's public values.
# Set these to point a build at a different Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Set to "true" to build the sample-data demo instead of the live app.
VITE_DEMO_MODE=
